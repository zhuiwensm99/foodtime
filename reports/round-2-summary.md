# 食光 FoodTime 家庭 Web 端 · Round 2 质量攻坚总结

- **周期**：2026-08-14 ~ 2026-08-15
- **目标**：移动端适配 + 残余 UX 项修复 + 测试用例回填（用户要求的"循环不少于三次"第二轮）
- **环境**：http://127.0.0.1:8790/
- **结论**：✅ 全部修复完成，关键用例执行 18/18 通过，单测 node --test 92/92 通过

## 一、本轮修复项（前端 / UX）

| 编号 | 角色 | 问题 | 修复 |
| --- | --- | --- | --- |
| U-5 / U-8 | UX + 前端 | 进入偏好/通知/展示等非食材视图时，AI 助手浮窗（#quickAgentFab）仍遮挡内容 | `setView()` 中对非 home/items 视图显式 `add("hidden")`，离开时 `remove("hidden")`；验证 preferences 下 fab `display:none` |
| U-7 | 前端 | 库存表格在移动端（≤760px）出现横向滚动、操作按钮过小 | 移除 `.inventory-table{overflow-x:auto}` 横向滚动规则；≤560px 下操作按钮改为卡片式 flex 布局，每个按钮 `min-height:44px`、`flex:1 1 calc(50% - 4px)`，编辑按钮占满整行 |
| U-4 | UX | 菜谱卡片书图标与彩色背景融合、看不清 | 新增 `.recipe-img .ic` 白底圆角方块 + 阴影，提升图标对比度 |
| 缓存 | 前端 | 修复后浏览器仍用旧 CSS/JS | index.html 引用版本号 `20260812-1` → `20260815-1`（CSS/JS 共 6 处），并同步 `test/auth-ui.test.js` 5 处断言 |
| U-6 | 前端 | 在"已处理"tab 打开添加弹窗会自动切回"全部" | 验证 `openFoodEditor` 触发时 `state.foodList.filter` 由 handled 复位为 all 并重渲染（符合预期，无需改代码） |

## 二、测试用例执行回填

- 新建 `execute-test-cases.js`（Playwright 驱动 18 条关键用例），覆盖：登录三态、添加食材、状态 tab、处理(吃掉)、已处理列表、恢复、台账/菜谱/预警/账户/偏好/通知/展示视图可达性、两项 API 鉴权。
- 首跑 17/18，唯一失败 **FE-020 添加食材** → 经排查为**测试脚本缺陷**：断言读取的 toast 元素选择器错误（`#toast` 实际不存在，真实 toast 元素 id 为 `#message`），且行数断言 `rows>0` 恒真不具判别力。
- 修正：① `getToast()` 读取 `#message`；② FE-020 改为"添加前行数 vs 添加后行数增量 + 新食材名称出现在列表"的确定性断言。
- 修正后重跑 **18/18 全部通过**。

## 三、质量门禁

| 检查项 | 结果 |
| --- | --- |
| 关键用例执行（Playwright） | 18/18 通过 |
| 单元测试 node --test | 92/92 通过 |
| 控制台错误（走查期） | 0 |
| 移动端（375px）视图可达 + 无横向溢出 | 通过（Round 3 复测确认） |

## 四、遗留 / 进入 Round 3

- Round 2 已闭合用户的 4 类原始问题（添加无响应、已处理为空、恢复状态错误、移除演示账号）及移动端/UX 残余项。
- 进入 Round 3：产品经理做最终全面复核（全视图死页/空页走查 + 全交互回归 + 生产发布判定）。
